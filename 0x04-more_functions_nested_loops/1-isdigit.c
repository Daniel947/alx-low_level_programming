#include "main.h"
/**
 * _isdigit - function for checking if input is digit
 *
 * Return: 0 or 1.
 * @c: input parameter  for _isdigit 
 * Author: Daniel Yamoah
 */
int _isdigit(int c)
{
		return (c >= 48 && c <= 57);
}
